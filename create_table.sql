CREATE TABLE questionnaire_responses (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    email VARCHAR(255),
    age INT,
    knowledge_level VARCHAR(50),
    philosophers VARCHAR(255),
    interest TEXT
);